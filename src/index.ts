//console.log("testing");
class TreeNode {
  data: number;
  leftChild: TreeNode;
  rightChild: TreeNode;
  constructor(data, leftChild, rightChild) {
    this.data = data;
    this.leftChild = leftChild;
    this.rightChild = rightChild;
  }
}

class Tree {
  method;
}
